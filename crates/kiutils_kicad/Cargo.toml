[package]
name = "kiutils_kicad"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

description = "KiCad typed AST and file codecs built on kiutils_sexpr"

[dependencies]
thiserror.workspace = true
kiutils_sexpr = { path = "../kiutils_sexpr" }
